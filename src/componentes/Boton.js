import React from "react";
import "../style.css";

export default class Boton extends React.component {
    constructor (props) {
        super(props);
        this.state = {};       
    }
    render () {
        return <button> </button>
    }
}